import {KeyboardAction, MouseAction, ScreenAction, WindowAction,} from "./lib";

export {
    KeyboardAction as DefaultKeyboardAction,
    MouseAction as DefaultMouseAction,
    ScreenAction as DefaultScreenAction,
    WindowAction as DefaultWindowAction,
}